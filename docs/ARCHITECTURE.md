# AI Proxy — Полная схема работы

## Общий поток (ASCII-диаграмма)

```
 ┌─────────┐                                                        ┌──────────────────┐
 │  Агент  │  (Claude Code, Pi, aider, npm, curl, ...)               │  API провайдер    │
 │ (клиент)│                                                        │ api.deepseek.com │
 └────┬────┘                                                        └────────▲─────────┘
      │                                                                      │
      │  HTTPS_PROXY=http://proxy.example.com:8443                            │
      │                                                                      │
      ▼                                                                      │
 ┌─────────────────────────────────────────────────────────────────────────────┐
 │                          AI PROXY (proxy.example.com:8443)                   │
 │                                                                             │
 │  ФАЗА 1: TCP-соединение                                                     │
 │  ┌───────────────────────────────────────────────────────────────────┐     │
 │  │ main.rs: accept TCP → tokio::spawn(handle_connection)             │     │
 │  │ mitm.rs: peek first bytes → CONNECT or plain HTTP?                │     │
 │  └──────────────────────────────┬────────────────────────────────────┘     │
 │                                 │                                          │
 │              ┌──────────────────┴──────────────────┐                       │
 │              ▼                                     ▼                       │
 │  ┌──────────────────────┐          ┌─────────────────────────────┐         │
 │  │  CONNECT (HTTPS)     │          │  Plain HTTP                 │         │
 │  │                      │          │  (httpbin.org, install.sh)  │         │
 │  │  ФАЗА 2: SSL Bump    │          │                             │         │
 │  │  ┌────────────────┐  │          │  handle_plain_http():       │         │
 │  │  │ host = 127.*?  │  │          │  /ca.pem → отдать CA        │         │
 │  │  │  ДА → туннель  │──┼──────────┼─>/install.sh → скрипт       │         │
 │  │  │  НЕТ → bump    │  │          │  /proxy.pac → PAC            │         │
 │  │  └──────┬─────────┘  │          │  / → портал HTML            │         │
 │  │         │            │          │  остальное → forward        │         │
 │  │  cert_cache.get_or_sign(host)   └──────────────┬──────────────┘         │
 │  │  (LRU 256, CA из certs/ca.pem)                 │                        │
 │  │         │                                      │                        │
 │  │  TlsAcceptor → handshake с клиентом            │                        │
 │  │  ALPN: принудительно http/1.1                  │                        │
 │  │         │                                      │                        │
 │  └─────────┼──────────────────────────────────────┘                        │
 │            │                                                               │
 │            ▼                                                               │
 │  ФАЗА 3: HTTP-запрос внутри туннеля                                        │
 │  ┌───────────────────────────────────────────────────────────────────┐     │
 │  │  hyper http1::Builder → service_fn → forward_request()            │     │
 │  │                                                                   │     │
 │  │  forward.rs:                                                      │     │
 │  │  1. resolve_target(host) → TargetConfig (из config.toml)         │     │
 │  │  2. is_known_target? → dpi=true/false                            │     │
 │  │  3. Buffer body (max 10 MB)                                      │     │
 │  │  4. JSON parse: serde_json::from_str(body)                       │     │
 │  └──────────────────────────────┬────────────────────────────────────┘     │
 │                                 │                                          │
 │                                 ▼                                          │
 │  ФАЗА 4: DPI токенизация (только для AI-доменов из config.toml)            │
 │  ┌───────────────────────────────────────────────────────────────────┐     │
 │  │  dpi.rs: DpiEngine::scan(text) → Vec<Detection>                   │     │
 │  │                                                                   │     │
 │  │  ┌─────────────────┐  ┌──────────────┐  ┌─────────────────────┐   │     │
 │  │  │ SECRET          │  │ PII          │  │ JSON-aware          │   │     │
 │  │  │ ───────         │  │ ───          │  │ ───────────         │   │     │
 │  │  │ API_KEY_REGEX   │  │ FIO regex    │  │ serde_json парсит   │   │     │
 │  │  │ sk-/pk- +20sym  │  │ + dict 500+  │  │ только string       │   │     │
 │  │  │ KV: key=value   │  │   имён       │  │ значения внутри     │   │     │
 │  │  │ AhoCorasick     │  │ PHN валидатор│  │ JSON, не трогая     │   │     │
 │  │  │ prefixes        │  │ EML regex    │  │ ключи/структуру     │   │     │
 │  │  └────────┬────────┘  └──────┬───────┘  └──────────┬──────────┘   │     │
 │  │           └──────────┬───────┴──────────────────────┘              │     │
 │  │                      ▼                                            │     │
 │  │  Для каждой Detection:                                            │     │
 │  │    token = SHA256(value + session)[:6]                            │     │
 │  │    masked = ‹KEY_b7e548› / ‹FIO_766ac9› / ‹EML_75d961› / ...      │     │
 │  │    vault.set(token → original)  (Redis, TTL 1h)                  │     │
 │  │    token_map[token] = original                                   │     │
 │  │                      │                                            │     │
 │  │  tokenize_json_body() → masked_body (с токенами)                 │     │
 │  └──────────────────────┬────────────────────────────────────────────┘     │
 │                         │                                                  │
 │  ФАЗА 5: Audit (асинхронно, батч 5с)                                       │
 │  ┌───────────────────────────────────────────────────────────────────┐     │
 │  │  audit.rs: AuditChannel.send(event) → mpsc channel                │     │
 │  │                                                                   │     │
 │  │  Фоновый task: каждые 5с выводит батч:                            │     │
 │  │  "AUDIT BATCH: 12 violations in last 5s: SECRET=8 FIO=2 PHN=2"   │     │
 │  │                                                                   │     │
 │  │  Каждое событие:                                                  │     │
 │  │    type: SECRET / PII_FIO / PII_PHONE / PII_EMAIL / PII_COMPANY  │     │
 │  │    masked: sk-*** / Иван И*** / +7 999 ***-**-67                 │     │
 │  │    token: ‹KEY_b7e548›                                            │     │
 │  │    host: api.deepseek.com                                         │     │
 │  └───────────────────────────────────────────────────────────────────┘     │
 │                         │                                                  │
 │                         ▼                                                  │
 │  ФАЗА 6: Отправка upstream                                                │
 │  ┌───────────────────────────────────────────────────────────────────┐     │
 │  │  forward.rs: send_upstream()                                      │     │
 │  │                                                                   │     │
 │  │  1. TLS connect к target.host:443 (make_tls_connector)           │     │
 │  │  2. hyper Request::builder()                                      │     │
 │  │     - method, path, headers (из оригинала)                       │     │
 │  │     - body = masked_body (с токенами ‹KEY_xxx›)                  │     │
 │  │     - content-length = len(masked_body) ← пересчитан!            │     │
 │  │  3. ALPN: http/1.1                                                │     │
 │  │  4. Отправка → ожидание ответа                                    │     │
 │  └───────────────────────────────┬───────────────────────────────────┘     │
 │                                  │                                         │
 └──────────────────────────────────┼─────────────────────────────────────────┘
                                    │
                                    ▼
                          ┌─────────────────────┐
                          │  api.deepseek.com   │
                          │  Принимает запрос   │
                          │  с токенами вместо  │
                          │  реальных секретов  │
                          └─────────┬───────────┘
                                    │
                                    │ Response (SSE stream)
                                    ▼
 ┌─────────────────────────────────────────────────────────────────────────────┐
 │                          AI PROXY — ОБРАТНЫЙ ПУТЬ                            │
 │                                                                             │
 │  ФАЗА 7: Получение ответа от upstream                                       │
 │  ┌───────────────────────────────────────────────────────────┐             │
 │  │  Response<String> от upstream                             │             │
 │  │  Статус, заголовки, тело (SSE chunks для streaming API)   │             │
 │  └──────────────────────────┬────────────────────────────────┘             │
 │                             │                                              │
 │                             ▼                                              │
 │  ФАЗА 8: Детокенизация                                                      │
 │  ┌───────────────────────────────────────────────────────────────────┐     │
 │  │  forward.rs: detokenize_if_needed()                               │     │
 │  │                                                                   │     │
 │  │  1. Проверить: есть ли token_map? (были токены в запросе?)       │     │
 │  │  2. Regex: \$([A-Z]+_[a-f0-9]{6})\$                              │     │
 │  │  3. Для каждого найденного токена:                                │     │
 │  │     - Локально: token_map[token] → original                       │     │
 │  │     - ИЛИ: vault.get(token) → original (Redis lookup)            │     │
 │  │  4. Заменить все токены на оригинальные значения                  │     │
 │  │                                                                   │     │
 │  │  Пример:                                                          │     │
 │  │    Было:  "Используй ключ ‹KEY_b7e548› для API"                  │     │
 │  │    Стало:  "Используй ключ sk-abc123... для API"                 │     │
 │  └──────────────────────────────┬────────────────────────────────────┘     │
 │                                 │                                          │
 │                                 ▼                                          │
 │  ФАЗА 9: Логирование                                                         │
 │  ┌───────────────────────────────────────────────────────────────────┐     │
 │  │  log_final() — span.enter() для синхронной секции                 │     │
 │  │                                                                   │     │
 │  │  DPI=true:  DEBUG — полные заголовки + тело ответа               │     │
 │  │  DPI=false: TRACE — минимально (только статус + длина)           │     │
 │  │  Compressed: [compressed, N bytes] вместо бинарного мусора       │     │
 │  │                                                                   │     │
 │  │  INFO:  POST /v1/chat → api.deepseek.com status=200 len=1567    │     │
 │  │         duration=245ms                                            │     │
 │  └──────────────────────────────┬────────────────────────────────────┘     │
 │                                 │                                          │
 │                                 ▼                                          │
 │  ФАЗА 10: Ответ клиенту                                                     │
 │  ┌───────────────────────────────────────────────────────────────────┐     │
 │  │  hyper Response → TLS stream → клиент                             │     │
 │  │  Клиент получает оригинальные данные (детокенизированные)         │     │
 │  └───────────────────────────────────────────────────────────────────┘     │
 │                                                                             │
 └─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
                              ┌──────────┐
                              │  Агент   │
                              │ получает │
                              │ ответ    │
                              └──────────┘
```

---

## Фазы кратко

| # | Фаза | Файл | Что происходит |
|:--|:-----|:-----|:---------------|
| 1 | TCP accept | `main.rs` | Клиент подключается к прокси, spawn задачи |
| 2 | CONNECT / SSL Bump | `mitm.rs` | Peek → CONNECT? → loopback tunnel или cert_cache.get_or_sign → TLS handshake |
| 3 | HTTP parse | `mitm.rs` → `forward.rs` | hyper парсит HTTP-запрос внутри туннеля |
| 4 | DPI токенизация | `dpi.rs` | Сканирование тела: секреты → `‹KEY_xxx›`, ФИО → `‹FIO_xxx›`, и т.д. |
| 5 | Audit | `audit.rs` | Асинхронная отправка событий в канал, батч каждые 5с |
| 6 | Upstream | `forward.rs` | TLS connect к API, отправка токенизированного тела |
| 7 | Response | `forward.rs` | Получение ответа (включая SSE streaming) |
| 8 | Детокенизация | `forward.rs` | Regex по `‹TOKEN›` → замена на оригинал из token_map/vault |
| 9 | Логирование | `forward.rs` | DPI=DEBUG, passthrough=TRACE, сжатое=[compressed, N bytes] |
| 10 | Ответ клиенту | `mitm.rs` | Готовый ответ → TLS → клиент |

---

## Типы токенов

```
‹KEY_b7e548›  ← Secret     (sk-xxx, токены, пароли)
‹FIO_766ac9›  ← PiiFio     (Иван Иванов)
‹ORG_3f2a1b›  ← PiiCompany (Acme Corp, Globex Inc)
‹EML_75d961›  ← PiiEmail   (user@company.com)
‹PHN_ee84ac›  ← PiiPhone   (+7 999 123-45-67)
```

Токен = `SHA256(оригинал + session_id)[:6]` — стабильный, необратимый без vault.

---

## Хранилище токенов

```
Запрос:  sk-abc123 → ‹KEY_b7e548›
         ↓
    ┌─────────────────────────────┐
    │  Vault (Redis, TTL 1 час)   │
    │                             │
    │  KEY_b7e548 → sk-abc123     │  ← сохраняется при токенизации
    │  FIO_766ac9 → Иван Иванов   │
    │  EML_75d961 → a@b.com       │
    └─────────────────────────────┘
         ↓
Ответ:  ‹KEY_b7e548› → sk-abc123  ← восстанавливается при детокенизации
```

---

## DPI фильтры (точность)

```
Входной текст
    │
    ├──→ SECRET (AhoCorasick + regex)
    │    ├── sk-/pk- + 20 символов        ← API_KEY_REGEX
    │    ├── key: value (12+ символов)    ← SECRET_KV_REGEX
    │    └── bearer / token / password    ← SECRET_PREFIXES
    │
    ├──→ PII_FIO (regex + словарь имён)
    │    ├── [А-ЯЁ][а-яё]+ [А-ЯЁ][а-яё]+  ← regex кандидат
    │    └── проверка: 1-е ИЛИ 2-е слово   ← names_dict (500+ имён)
    │        в словаре? НЕТ → пропуск
    │
    ├──→ PII_PHONE (regex + валидатор)
    │    ├── (?:\+7|8) \d{3} \d{3} ...    ← regex кандидат
    │    └── 11 цифр + код региона РФ      ← is_valid_russian_phone()
    │        код 1XX? НЕТ → пропуск
    │
    ├──→ PII_EMAIL (regex)
    │    └── \b[\w.+-]+@[\w.-]+\.\w{2,}\b  ← высокая точность
    │
    └──→ PII_COMPANY (regex)
         └── ООО|ЗАО|ИП|LLC ...            ← по юридическому префиксу
```

---

## Loopback обход

```
CONNECT localhost:11434  (Ollama)
CONNECT 127.0.0.1:3000   (MCP server)
         │
         ▼
    is_loopback(host)?
    ├── ДА → tunnel_tcp() напрямую, без SSL bump, без DPI
    └── НЕТ → SSL bump → DPI → forward
```

---

## Безопасность

```
CA сертификат (certs/ca.pem)
    │
    ├── НИКОГДА не перегенерируется (проверка при старте)
    ├── Путь: config.ca_absolute_dir() — относительно config.toml
    │
    ├── Подписывает per-domain сертификаты
    │   cert_cache: LRU(256)
    │     api.deepseek.com → leaf cert (CA-signed)
    │     api.openai.com   → leaf cert (CA-signed)
    │     ...
    │
    └── Клиент должен доверять CA (apx install)
       否则: tls handshake eof
```
